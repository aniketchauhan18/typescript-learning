function addTwo(num: number, age?:number): number {
  return num + 2;
}

function getUpper(value: string) {
  return value.toUpperCase();
}

function signUpUser(name: string, email: string, isPaid: boolean) {
   return name = "Aniket", email = "sajbva@h.com", isPaid = false
}

addTwo(5); 
getUpper("aniket");

export {};